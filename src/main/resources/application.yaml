server:
  address: ${SERVER_ADDRESS:127.0.0.1}
  port: ${SERVER_PORT:8080}

spring:
  config:
    import: optional:file:.env[.properties]

  elasticsearch:
    uris: ${ELASTICSEARCH_URIS:http://127.0.0.1:9200}
    connection-timeout: ${ELASTICSEARCH_CONNECT_TIMEOUT:1s}
    socket-timeout: ${ELASTICSEARCH_READ_TIMEOUT:3s}

  flyway:
    enabled: true
    locations: classpath:db/migration

  sql:
    init:
      mode: never

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
    show-sql: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 11MB

jwt:
  secret: ${JWT_SECRET}

app:
  search:
    mode: ${APP_SEARCH_MODE:like}
    backend: ${APP_SEARCH_BACKEND:postgres}
    indexing-enabled: ${APP_SEARCH_INDEXING_ENABLED:false}
    fallback-enabled: ${APP_SEARCH_FALLBACK_ENABLED:true}
  storage:
    upload-root: ${UPLOAD_PATH:./uploads}
  cors:
    allowed-origins: []

management:
  endpoints:
    enabled-by-default: false
    web:
      exposure:
        include: health
  endpoint:
    health:
      enabled: true
      probes:
        enabled: false
      show-components: never
      show-details: never
